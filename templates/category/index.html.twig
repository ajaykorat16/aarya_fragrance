{% extends 'base.html.twig' %}

{% block title %}Category index{% endblock %}
{% block page_title %}<h3>Category index</h3>
{% endblock %}
{% block create %}
    <form action="{{ path('app_category_new') }}">
        <button class="btn btn-primary" type="submit">Create</button>
    </form>
{% endblock %}

{% block body %}
<div class="table-responsive">
    <table class="table border mb-0">
        <thead class="table-dark">
        <tr class="align-middle">
                <th class="text-center">Id</th>
                <th class="text-center">Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categories %}
            <tr class="align-middle">
                <td class="text-center">{{ category.id }}</td>
                <td class="text-center">{{ category.name }}</td>
                <td>
                    <a href="{{ path('app_category_show', {'id': category.id}) }}"><i class="fa-solid fa-eye"></i></a>
                    <a href="{{ path('app_category_edit', {'id': category.id}) }}"><i class="fa-solid fa-pencil"></i></a>
                    <a href="{{ path('app_category_delete', {'id': category.id}) }}"><i class="fa-solid fa-trash"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
